# Notas finais

A rota oficial permanece `/design-pdf`.

O módulo principal permanece em `src/features/design-pdf`.

O adaptador legado `src/lib/pdf/utils/coverSvgEngine.ts` continua existindo para chamadas antigas, mas delega para o novo engine.
